# backlog/ — work not started

One file per task, template `kernel/templates/TASK.md`, named
`T####-short-slug.md` (sequential, never reused). Anything discovered
mid-session that isn't the current task lands here as a stub (LAW L5) —
one honest line beats a perfect ticket.

Promotion to `../active/` happens deliberately (P04 pre-flight), max 3 active.
