# setup/ — the weird stuff a fresh machine needs

One note per quirk (P11 step 5): board drivers, udev rules, PATH surgery,
license servers, "run X once before Y works", pinned versions and WHY.

The test this folder must pass: could the human rebuild the dev machine from
`env/` alone, without archaeology through old chat logs?
