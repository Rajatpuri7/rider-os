# LESSONS — mistakes we refuse to repeat

> Append-only. Read before debugging (P08 step 0 checks here) and harvested
> at audits for kernel improvements (LAW L12). A lesson that repeats means the
> kernel needs a change (META.md), not another lesson entry.

Format per entry:

```
## YYYY-MM-DD — <one-line lesson>
- What happened: <2–3 lines>
- Root cause class: <wiring | assumption | process-skip | tool | knowledge-gap | ...>
- Rule of thumb going forward: <the reusable heuristic>
- Should the kernel change? no / proposed → <link to META proposal>
```

---

*(no entries yet)*
